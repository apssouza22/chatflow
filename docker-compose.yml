
services:
  redis:
    image: redis/redis-stack:6.2.6-v10
    volumes:
      - ./redis-data:/data/
    ports:
      - "6379:6379"
      - "8001:8001"

volumes:
  redis:
    driver: local
  postgres:
    driver: local

